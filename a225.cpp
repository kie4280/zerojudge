#include <iostream>

using namespace std;

int main() {
    int num;
    while(cin>>num) {
        int arr[num];
        for(int a=0;a<num;a++) {
            cin>>arr[a];
        }
    }
}
